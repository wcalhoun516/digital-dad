# All LLM calls route through the local-llm-conductor at http://127.0.0.1:8080.
# No keys are needed locally; T3 (--remote) needs OPENROUTER_API_KEY set in the
# conductor's own .env, not here. This file is intentionally left empty.
